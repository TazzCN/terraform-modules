# Terraform files
# .terraform/
.terraform.lock.hcl
# .tfstate files
*.tfstate
*.tfstate.backup
# Crash log files   
crash.log
# Exclude override files as they are usually used to override resources locally and so don't need to be shared
override.tf